import { MediaYearPipe } from './media-year.pipe';

describe('MediaYearPipe', () => {
  it('create an instance', () => {
    const pipe = new MediaYearPipe();
    expect(pipe).toBeTruthy();
  });
});
