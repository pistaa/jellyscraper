import { MediaTitlePipe } from './media-title.pipe';

describe('MediaTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new MediaTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
